package day12;

/* 1. 기반(베이스코드) 제공 */
public abstract class Shape {					//2. abstract 추상 클래스 = 상속 받아야한다.
	public abstract double getArea(double r);	//3. abstract 추상 메소드 = 메소드의 선언부 결정됨 = getArea가 고정 = 틀이 고정된다.
	
}
